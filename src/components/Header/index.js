import * as Styled from './styles';

const Header = () => {
  return (
    <Styled.Container>
      <Styled.Header>
        <Styled.Title>Financial App</Styled.Title>
      </Styled.Header>
    </Styled.Container>
  );
};

export default Header;
